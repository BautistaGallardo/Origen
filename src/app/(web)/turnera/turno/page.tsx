import Turno  from "./scheduleCalendar";

export default function Turnos() {

    return (
        <main className="h-screen flex bg-slate-50">
            <div className="m-auto">
                <Turno />
            </div>
        </main>
    );
}